// routes/user.js
const express = require('express');
const router = express.Router();

// Add user-related routes here later (e.g., profile updates)
// For now, keep it empty to avoid errors.

module.exports = router; // ✅ Export the router